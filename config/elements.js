/**
 * All the common element except pages were writen in this file
 *
 * How to use
 *
 * const elements = require('../../config/globals');
 * or
 * const elements = require('../../config/elements')
 * this.click(elements.home.version)
 */

var home = {
    verison: 'body .navbar-header img'
};

module.exports = {
    home
};
